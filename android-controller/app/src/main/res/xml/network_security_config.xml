<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security configuration for Kinetix Controller.
    Allows cleartext (ws://) traffic to local/private network IPs
    where the PC server runs. Required for Android 9+ (API 28+).
-->
<network-security-config>
    <!-- Allow cleartext to private/local network addresses -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>

    <!-- Fallback: also allow cleartext globally (user may use any IP) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
